  name: "Mi first workflow"
  on:
    workflow_dispatch:
    
    push:
      branches:
        - main
        
    
  jobs:
    compile:
      runs-on: ubuntu-lates
      steps:
        - name: Checkout
          uses: actions/checkout@v3.5.3
        
        - name: compile
          run: npx tsc
